pypi:
  build: pypi
nginx:
  image: nginx    
  links:
  - pypi
  volumes:
  - ./pypi/conf/nginx.server.conf:/etc/nginx/conf.d/default.conf
  ports:
  - 80:80
nginx_default:
  image: nginx
  volumes:
  - ./nginx/conf/nginx.conf:/etc/nginx.conf
  - ./nginx/html:/usr/share/nginx/html/
  ports:
  - 8080:80

